- [x] 1. 编辑 `admin-frontend/src/views/index.vue`，清除多余的广告、技术选型列表和更新日志，替换为“欢迎使用星空集市控制台”等简洁文案。
- [x] 2. 在 `admin-frontend` 目录下全局搜索“若依”，将所有显示的系统名称替换为“星空集市控制台”（包括 `.env` 环境文件中的 `VUE_APP_TITLE`，`package.json` 中的 `description` 和 `name` 等）。
- [ ] 3. 运行项目格式化并构建/运行测试，验证页面正常展示无报错。
