# Tasks: Membership System

- [x] 更新 `docs/产品需求文档_PRD.md`，将“信用体系”相关描述替换为“会员体系”及对应逻辑。
- [x] 数据库设计：创建 `mkt_membership_level` 配置表；在用户表中增加积分（points）和当前等级（level_id）字段。
- [ ] 重新修改：更新 `docs/产品需求文档_PRD.md` 和设计文档，将会员规则修改为：退款时扣除对应积分，且**允许降级**，以防止刷级。
- [ ] 重新修改：更新数据库，不再依赖 `historical_max_points` 判定等级，直接基于 `points` 余额。
- [ ] 后端：实现B端 `mkt_membership_level` 相关的 CRUD 配置接口。
- [ ] 后端：实现订单“已完成”时的积分增加、等级重估逻辑。
- [ ] 后端：实现订单全额退款时的积分扣除、防负数及**等级重估降级**逻辑。
- [ ] 后端：修改订单结算逻辑，在创建订单时应用用户当前等级对应的折扣率。
- [ ] 联调：将B端前端真实的API替换现有的Mock数据。
- [x] B端前端：开发会员等级及折扣配置管理页面。
- [x] C端前端：修改个人中心页面，展示用户的当前积分与会员等级。
- [x] C端前端：修改商品详情页/下单页，提示并应用会员等级专属折扣。

<!-- review skipped: skill unavailable -->
